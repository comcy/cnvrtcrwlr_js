{
    "name": "downcrwlr_js",
    "version": "0.1.0",
    "description": "This is minimal markdown to html parser which can interprete any markdown input.",
    "main": "index.js",
    "scripts": {
        "copy": "node tools/copy-files.js",
        "cleanup": "node tools/cleanup-package.js",
        "publish": "npm publish dist",
        "build": "npm run rimraf:dist && npm run copy && npm run cleanup",
        "rimraf:dist": "rimraf ./dist"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "del": "^2.2.2",
        "rimraf": "^2.6.1",
        "rollup": "^0.51.8",
        "rollup-plugin-commonjs": "^8.2.6",
        "rollup-plugin-node-resolve": "^3.0.0"
    }
}
